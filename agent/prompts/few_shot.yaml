# Few-Shot Examples

## Example 1: Revenue by State
**Question**: "What is the total revenue by customer state?"

```sql
SELECT 
    c.customer_state,
    SUM(oi.price) as total_revenue
FROM orders o
JOIN customers c ON o.customer_id = c.customer_id
JOIN order_items oi ON o.order_id = oi.order_id
WHERE o.order_status = 'delivered'
GROUP BY c.customer_state
ORDER BY total_revenue DESC
LIMIT 27;
```

## Example 2: Average Order Value by Category
**Question**: "Show average order value by product category"

```sql
SELECT 
    COALESCE(pct.product_category_name_english, p.product_category_name) as category,
    AVG(oi.price + oi.freight_value) as avg_order_value
FROM order_items oi
JOIN products p ON oi.product_id = p.product_id
LEFT JOIN product_category_translation pct 
    ON p.product_category_name = pct.product_category_name
GROUP BY COALESCE(pct.product_category_name_english, p.product_category_name)
ORDER BY avg_order_value DESC
LIMIT 20;
```

## Example 3: Top Sellers by Revenue
**Question**: "Who are the top 10 sellers by total revenue?"

```sql
SELECT 
    s.seller_id,
    s.seller_city,
    s.seller_state,
    COUNT(DISTINCT oi.order_id) as order_count,
    SUM(oi.price) as total_revenue
FROM sellers s
JOIN order_items oi ON s.seller_id = oi.seller_id
GROUP BY s.seller_id, s.seller_city, s.seller_state
ORDER BY total_revenue DESC
LIMIT 10;
```

## Example 4: Distinct Customer Count
**Question**: "How many unique customers placed orders in 2017?"

```sql
SELECT 
    COUNT(DISTINCT c.customer_unique_id) as unique_customers
FROM orders o
JOIN customers c ON o.customer_id = c.customer_id
WHERE o.order_purchase_timestamp >= '2017-01-01'
  AND o.order_purchase_timestamp < '2018-01-01';
```

## Example 5: Geolocation Join (Using geolocation_by_zip)
**Question**: "What is the average order value by customer city coordinates?"

```sql
SELECT 
    gz.geolocation_city,
    gz.geolocation_state,
    gz.geolocation_lat,
    gz.geolocation_lng,
    AVG(oi.price + oi.freight_value) as avg_order_value
FROM orders o
JOIN customers c ON o.customer_id = c.customer_id
JOIN geolocation_by_zip gz ON c.customer_zip_code_prefix = gz.geolocation_zip_code_prefix
JOIN order_items oi ON o.order_id = oi.order_id
GROUP BY gz.geolocation_city, gz.geolocation_state, gz.geolocation_lat, gz.geolocation_lng
ORDER BY avg_order_value DESC
LIMIT 50;
```

## Example 6: Lead Conversion (Time to Delivery)
**Question**: "What is the average delivery time in days by state?"

```sql
SELECT 
    c.customer_state,
    AVG(
        EXTRACT(DAY FROM o.order_delivered_customer_date - o.order_purchase_timestamp)
    ) as avg_delivery_days
FROM orders o
JOIN customers c ON o.customer_id = c.customer_id
WHERE o.order_status = 'delivered'
  AND o.order_delivered_customer_date IS NOT NULL
GROUP BY c.customer_state
ORDER BY avg_delivery_days DESC
LIMIT 27;
```

## Example 7: Deduplicated Reviews
**Question**: "What is the average review score, considering only the latest review per order?"

```sql
WITH latest_reviews AS (
    SELECT DISTINCT ON (order_id)
        order_id,
        review_score
    FROM order_reviews
    ORDER BY order_id, review_creation_date DESC
)
SELECT 
    AVG(review_score) as avg_review_score
FROM latest_reviews;
```
